<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            width: 100vw;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #14069457;
            color: palegoldenrod;
            flex-direction: column;
        }
        .tables-container{
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
        }
        .table-holder{
            text-align: center;
            width: 33%;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        table{
            width: 60%;
        }
        tbody{
            height: 350px;
        }
    </style>
</head>
<body>
    <h1>Time and Day of Most Registers</h1>
    <div class="tables-container">
        <div class="table-holder">
            <table>
                <th>Hours Of Day</th><th>number of registers</th>
                <% hours_ranges.each do |hour, registers| %>
                    <tr>
                        <td><%= "#{hour}"%></td>
                        <td><%= "#{registers}"%></td>
                    </tr>
                <% end %>
            </table>
        </div>
        <div class="table-holder">
            <table>
                <th>individual hours</th><th>number of registers</th>
                <% individual_hours.each do |hour, registers| %>
                    <tr>
                        <td><%= "#{hour}:00 to #{hour.to_i + 1}:00"%></td>
                        <td><%= "#{registers}"%></td>
                    </tr>
                <% end %>
            </table>
        </div>
        <div class="table-holder">
            <table>
                <th>Day of The Week</th><th>number of registers</th>
                <% days.each do |day, registers| %>
                    <tr>
                        <td><%= "#{day}"%></td>
                        <td><%= "#{registers}"%></td>
                    </tr>
                <% end %>
            </table>
        </div>
    </div>
</body>
</html>
